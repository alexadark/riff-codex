{
  "name": "riff-dashboard",
  "version": "0.1.0",
  "description": "Local web dashboard for the RIFF framework.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun run --hot server.ts"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "yaml": "^2.6.0",
    "chokidar": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}

interface:
  display_name: "RIFF Promote"
  short_description: "Reserve promotion for production scope changes"
  default_prompt: "Use $promote through $riff:promote only for an explicit scratch-to-production scope change."
